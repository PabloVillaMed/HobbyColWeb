<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!-- Debug builds only: never present in a release APK. -->
    <activity
        android:name=".WidgetPreviewActivity"
        android:exported="true" />
  </application>
</manifest>
