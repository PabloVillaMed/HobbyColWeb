<?xml version="1.0" encoding="utf-8"?>
<!-- The habit history lives in the WebView's local storage, so it is what the
     system backup needs to carry to a new device. -->
<full-backup-content>
  <include domain="database" path="." />
  <include domain="file" path="app_webview" />
</full-backup-content>
